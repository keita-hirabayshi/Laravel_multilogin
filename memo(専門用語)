
インターフェース    ある要素とある要素をつなぐ機能のこと
ミドルウェア       OSとアプリケーション等をつなぐ複雑な事柄を処理するソフトウェアのこと (Webサーバー・アプリケーションサーバー・DBサーバーも含む)
                アプリケーションまでの処理過程における、一面(レイヤー)とみなしてよい

プロキシサーバー    クライアントからのリクエスト内容をチェックし、問題なければWebサーバーへ渡す。監視役

www                     ハイパーテキストと呼ばれる文書形式でデータをサーバーに格納し、ネット経由で閲覧できる機能を提供するサービスのこと
http                    webブラウザとwebサーバー間で、情報をやり取りする際の通信プロトコル(通信規則)。いわば、約束事のことをさす。TCP/IPと組み合わせて使用する。
httpステータスコード       1000:通信処理中  2000:通信成功  3000:リダイレクト  4000:クライアントエラー
ソフトデリート             リレーショナルデータベースにてデータを del_flg 等を使って有効・無効を選択すること。ここでは、実際に削除は行わない。
オーバーライド             オブジェクト指向のプログラミングにおいて、親モデルにて設定したメソッドを、子モデルで再定義しメソッドを上書き、機能を更新すること
slug                    wordpress等のサイトにて、語尾に特定の単語を追記し、URLの中身を把握しやすくする文字列ぼこと  
                        記事内容を掲載しているページならblogやarticleなど

暗黙の結合              値を引き継ぐ際に、 モデル名 $変数名 とすると、FindOrFailメソッドにて確認後に自動でモデル生成してくれる。
                      Route::get('/users/{user}', function (User $user) {
                            return $user->email;
                        });
*Missingメソッドを使えば、暗黙の結合で指定のモデルが見つからない時に404ページ以外のルートへリダイレクトしてあげられる。
                    Route::get('/locations/{location:slug}', [LocationsController::class, 'show'])
                        ->name('locations.view')
                        ->missing(function (Request $request) {
                            return Redirect::route('locations.index');
                        });

CSRF      クロスサイトリクエストフォージェリーは認証済みのユーザーに代わり、不正なリクエストを実行する攻撃の一種
      仕組み
      まず、ブラウザ側からサーバー側へトークンの生成を要求する。サーバー側はリクエストのあったクライアントの情報を確認のち、トークンを生成し渡す。
      次になんらかのデータをサーバー側へ送る際に、トークンの有無をサーバーが確認しリクエストを処理する。
      トークンはアクションごとに再生成され、ブラウザCookie内の「X-CSRF-TOKEN」に格納されている

レート制限  特定のユーザーが一定時間にアクセスできる回数を制限する。サーバーの負荷を和らげ、セキュリティ的にも良い RouteServiceProviderにて設定する
CORS      簡単にいえば、異なる環境でも接続できる様にすること。通常、通信プロトコルやポート名やホスト名が異なると、通信を受け付けない。
トークン   プログラミングにおいてコードを構成する最小単位のこと。認証済みのユーザーであることを示す際に発行される文字列を指すこともある。
CV        コンバーションは「成果」を意味する。具体的にはWebサイトなどへのアクセス数など

SPA       シングルページアプリケーションはWebアプリの構成法の一つ。ブラウザ側でページの移動を行わず、Web上のスクリプトがサーバー通信や画面推移を行う。
          何度も読み込みが行われないのが特徴。ただし、実装するにはそれなりの労力を要する
MPA       マルチページアプリケーションは、SPAとは異なり1リクエストごとに、WebサーバーにてHTMLの組み上げとCSSやJavacsriptをレスポンスで返す
          SPAと異なり処理量が多いので、ページの移動が遅くなりがち
SSR      サーバーサイドレンダリングは、ブラウザからのリクエストに応じて、WebサーバーがAPIと連携してHTMLの生成や返却を行う仕組み
SSG      SSRの内容を事前にAPIにストックさせておく仕組み。SSRより処理負荷が軽減される
*SSR,SSGともにSPIと連携されて使われる事が多い

サービスコンテナ    Laravelの中心的な機能。依存解決(ルートモデル結合等の先取り処理)やインスタンス化をカスタマイズできるといったメリットがある。
シングルトン       オブジェクト指向プログラミングにおける設計方法の一つ。実行時にインスタンスが単一になる
タイプヒント       引数の型を指定すること
PSR              HTTPのリクエストとレスポンスに関する仕様のこと。Laravelではデフォルトで別の仕様が使われている
ノーマライゼーション  正規化とも呼ぶ。データをある基準や形式に適合する様に変換すること。
JSON             Javascriptと相性の良い記載方法(HTMLも記載方法の一つ)
Cookie           パソコンやスマホ等のWebブラウザに一時的に保存される情報や仕組みのこと(ブラウザを閉じるとcookieが無効となる事が多い)
Eloquentモデル    SQLを書く量を少なくしつつ、DBに簡単にアクセスできる様な仕組みのこと
レンダ            レンダリングとも呼び、なんらかのデータ集合をもとに、一定の処理や演算を行い画像や映像、音声を生成する(webブラウザ上で画面を表示など)
コンパイル         プログラミング言語で書かれたコードを、コンピューターが実行可能なコードに変換すること
オンデマンド       何らかの要求があった場合に、それに応じて提供する形
オーバーヘッド     コンピューターがある処理を実行する際に、付随して実行する必要のある処理のこと
キャッシュ        よく使うデータを取り出しやすい様に準備しておく仕組みのこと
エンコード        何らかの文字列(日本語など)をPCが認識できる様な文字コードに置き換えてやること。作成側とブラウザ側の設定文字コードが異なると、正常に読み込まれずに文字化けが発生する
エスケープ        文字列を別の文字列に変換する。コードではないので、ブラウザでは読み込まれない
ループ変数        foreach文内にて、$loop->first等のメソッドにて使用する。これにより、特定の番号等でデータを取得する事が可能に
コンポーネント     いわばWebサイト等における、各構成要素を指す(ナブアイコンや目次・メインコンテンツなど)これらを個別に部品分けすることで、中身の把握がより容易になる
                中規模以上の複数人数でプロジェクトを作成する場合に便利な機能
webpack         複数のjavascriptファイルを1つにまとめ出力するツール
Laravel Mix     webpack上で機能し動作を容易にする、Laravelが提供するツール
Livewire        javascriptを一切書かずに、reactやvueのような非同期処理を実現できる。Laravelだけで、フロントエンドもカバーできる
モック           テスト環境へデプロイする際に、実際ののオブジェクトの動作をシミュレートしてくれるオブジェクト
SSR             Webサイトのサーバーにて、javascriptをレンダリングする事

Git	       パーソナルアクセストークンはユーザー名と合わせておく
	　　　　更新時は ユーザー名：上記　　パスワード：アクセストークン配列　で入力すると設定される